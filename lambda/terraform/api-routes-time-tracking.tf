# API Routes for Time Tracking Module
# NOTE: These routes are disabled until Lambda functions are deployed

# All time tracking API routes are commented out until the Lambda functions
# are deployed. Enable these routes after running the build and deploy
# process for the time tracking module.

# Placeholder for future time tracking API routes:
# - POST /time-tracking/sessions (manage_time_sessions)
# - GET /time-tracking/sessions/active (get_active_session)
# - GET /time-tracking/sessions (get_sessions)
# - GET /time-tracking/sessions/{id}/entries (get_session_entries)
# - GET /time-tracking/statistics (get_time_tracking_statistics)
# - GET /time-tracking/export (export_time_tracking_data)
# - GET /time-tracking/templates (get_time_tracking_templates)
# - POST /time-tracking/templates (create_time_tracking_template)
# - DELETE /time-tracking/templates/{id} (delete_time_tracking_template)
