# API Routes for Disputes Module
# NOTE: These routes are disabled until Lambda functions are deployed

# All dispute API routes are commented out until the Lambda functions
# are deployed. Enable these routes after running the build and deploy
# process for the disputes module.

# Placeholder for future dispute API routes:
# - POST /disputes (create_dispute)
# - GET /disputes (get_disputes)
# - GET /disputes/{id} (get_dispute)
# - PUT /disputes/{id}/status (update_dispute_status)
# - POST /disputes/{id}/close (close_dispute)
# - POST /disputes/{id}/escalate (escalate_dispute)
# - POST /disputes/{id}/mediation (request_mediation)
# - GET /disputes/{id}/messages (get_dispute_messages)
# - POST /disputes/{id}/messages (send_dispute_message)
# - POST /disputes/{id}/evidence (add_evidence)
# - POST /disputes/{id}/resolution/accept (accept_resolution)
