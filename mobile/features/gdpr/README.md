# GDPR Compliance Feature

Полнофункциональная система управления персональными данными в соответствии с GDPR (General Data Protection Regulation).

## Возможности

### 1. Экспорт данных (ExportDataScreen)

**Право на портативность данных (GDPR Статья 20)**

- ✅ Экспорт всех данных пользователя
- ✅ Выбор формата (JSON, CSV)
- ✅ Выборочный экспорт разделов
- ✅ Включение ссылок на файлы (24 часа)
- ✅ Скачивание и sharing экспортированных данных
- ✅ Rate limiting (1 экспорт в час)
- ✅ Детальная сводка экспортированных данных

**Доступные разделы для экспорта:**
- Профиль (личная информация)
- Заказы
- Отклики
- Проекты
- Отзывы (оставленные и полученные)
- Сообщения
- Уведомления
- Кошелек и транзакции
- Портфолио (для мастеров)

### 2. Удаление аккаунта (DeleteAccountScreen)

**Право на удаление (GDPR Статья 17 - "Право быть забытым")**

- ✅ Многоэтапный процесс удаления с предупреждениями
- ✅ Проверка возможности удаления
- ✅ Подтверждение паролем
- ✅ Выбор причины удаления
- ✅ Опциональная обратная связь
- ✅ Анонимизация данных
- ✅ 30-дневный период хранения резервной копии

**Что удаляется:**
- Профиль и личные данные
- Все сообщения
- История уведомлений
- Загруженные файлы
- История транзакций

**Что сохраняется (анонимно):**
- Отзывы (без личных данных)
- История заказов (для отчетности)

### 3. Управление согласиями (ConsentManagementScreen)

**Право на контроль обработки данных**

- ✅ Маркетинговые коммуникации
- ✅ Аналитика и улучшение сервиса
- ✅ Персонализация контента
- ✅ Сторонние сервисы
- ✅ Синхронизация с бэкендом
- ✅ История изменений

### 4. GDPR Информация (GDPRInfoScreen)

**Прозрачность и информирование (GDPR Статья 13)**

- ✅ Полная информация о правах пользователя
- ✅ Описание мер защиты данных
- ✅ Сроки хранения данных
- ✅ Быстрый доступ ко всем GDPR функциям
- ✅ Ссылки на политику конфиденциальности
- ✅ Контакт специалиста по защите данных

## Использование

### Навигация к GDPR экрану

```typescript
import { useRouter } from 'expo-router';

const router = useRouter();

// Открыть главный GDPR экран
router.push('/gdpr');
```

### Прямое использование компонентов

```typescript
import {
  GDPRInfoScreen,
  ExportDataScreen,
  DeleteAccountScreen,
  ConsentManagementScreen,
} from '../features/gdpr';

// Информация о GDPR
<GDPRInfoScreen
  onBack={() => router.back()}
  onNavigateToExport={() => {}}
  onNavigateToDelete={() => {}}
  onNavigateToConsents={() => {}}
/>

// Экспорт данных
<ExportDataScreen
  onBack={() => router.back()}
/>

// Удаление аккаунта
<DeleteAccountScreen
  onBack={() => router.back()}
  onDeleted={() => router.replace('/')}
/>

// Управление согласиями
<ConsentManagementScreen
  onBack={() => router.back()}
/>
```

## API

Все API методы доступны через `gdprApi`:

```typescript
import { gdprApi } from '../services/gdprApi';

// Экспорт данных
const data = await gdprApi.exportUserData({
  format: 'json',
  includeFiles: true,
  sections: ['profile', 'orders', 'reviews'],
});

// Удаление аккаунта
const result = await gdprApi.deleteUserAccount({
  confirmPassword: 'user_password',
  reason: 'privacy_concerns',
  feedback: 'Optional feedback',
});

// Получение согласий
const consents = await gdprApi.getConsentSettings();

// Обновление согласий
await gdprApi.updateConsentSettings({
  marketing: false,
  analytics: true,
  personalization: true,
  thirdParty: false,
});

// Проверка возможности удаления
const eligibility = await gdprApi.checkDeletionEligibility();

// Скачивание экспортированных данных
const content = await gdprApi.downloadExportedData(data, 'json');
```

## Типы данных

```typescript
// Экспортированные данные
interface GDPRExportData {
  exportInfo: {
    userId: string;
    exportedAt: string;
    format: 'json' | 'csv';
    requestedSections: string;
    dataRetentionPolicy: string;
  };
  profile?: { /* ... */ };
  orders?: any[];
  reviews?: { given: any[]; received: any[] };
  // ... другие разделы
  summary: {
    totalOrders: number;
    totalApplications: number;
    // ... другие счетчики
  };
}

// Результат операции
interface GDPROperationResult {
  success: boolean;
  message: string;
  operationId: string;
  timestamp: string;
  affectedRecords?: number;
  errors?: string[];
}

// Настройки согласий
interface ConsentSettings {
  marketing: boolean;
  analytics: boolean;
  personalization: boolean;
  thirdParty: boolean;
  updatedAt?: string;
}
```

## Соответствие GDPR

### Реализованные права

| Право | Статья GDPR | Реализация |
|-------|-------------|------------|
| Право на доступ | Статья 15 | Экспорт данных |
| Право на исправление | Статья 16 | Редактирование профиля |
| Право на удаление | Статья 17 | Удаление аккаунта |
| Право на ограничение | Статья 18 | Управление согласиями |
| Право на портативность | Статья 20 | Экспорт в JSON/CSV |
| Право на возражение | Статья 21 | Управление согласиями |

### Меры безопасности

- 🔒 Шифрование данных при передаче (HTTPS)
- 🔒 Шифрование данных при хранении
- 🔑 Подтверждение паролем для критических операций
- ⏱️ Rate limiting для предотвращения злоупотреблений
- 📝 Аудит логирование всех GDPR операций
- 🔔 Уведомления администраторов о критических операциях
- ⏰ Автоматическое удаление устаревших данных

### Сроки хранения данных

| Тип данных | Срок хранения |
|------------|---------------|
| Профиль | До удаления аккаунта |
| Сообщения | 1 год после последней активности |
| Транзакции | 7 лет (требование закона) |
| Логи | 90 дней |
| Удаленные данные | 30 дней (резервная копия) |

## Процесс удаления аккаунта

1. **Предупреждение** - Информация о последствиях
2. **Выбор причины** - Опциональная обратная связь
3. **Подтверждение** - Ввод пароля и текста "УДАЛИТЬ"
4. **Проверки** - Активные заказы, баланс кошелька
5. **Удаление** - Анонимизация данных
6. **Уведомление** - Email подтверждение
7. **Резервная копия** - 30 дней для восстановления
8. **Полное удаление** - Автоматически через 30 дней

## Процесс экспорта данных

1. **Выбор параметров** - Формат, разделы, файлы
2. **Rate limiting** - Проверка лимита (1 раз в час)
3. **Сбор данных** - Из всех источников
4. **Генерация файла** - JSON или CSV
5. **Подписанные URL** - Для файлов (24 часа)
6. **Уведомление** - Email для больших экспортов
7. **Скачивание** - Через приложение или sharing

## Интеграция в настройки профиля

```typescript
// В экране настроек профиля
import { useRouter } from 'expo-router';

const SettingsScreen = () => {
  const router = useRouter();

  return (
    <View>
      {/* ... другие настройки ... */}
      
      <TouchableOpacity onPress={() => router.push('/gdpr')}>
        <Text>Конфиденциальность и данные</Text>
      </TouchableOpacity>
    </View>
  );
};
```

## Локализация

Все тексты на русском языке. Для добавления других языков:

1. Создайте файлы переводов в `i18n/locales/`
2. Используйте `useTranslation` hook
3. Замените статические тексты на ключи переводов

## Тестирование

```typescript
// Тест экспорта данных
describe('GDPR Export', () => {
  it('should export user data', async () => {
    const data = await gdprApi.exportUserData({
      format: 'json',
      sections: ['profile'],
    });
    
    expect(data.exportInfo).toBeDefined();
    expect(data.profile).toBeDefined();
  });
});

// Тест удаления аккаунта
describe('GDPR Delete', () => {
  it('should delete account with valid password', async () => {
    const result = await gdprApi.deleteUserAccount({
      confirmPassword: 'correct_password',
      reason: 'not_using',
    });
    
    expect(result.success).toBe(true);
  });
});
```

## Troubleshooting

### Экспорт не работает

1. Проверьте подключение к интернету
2. Убедитесь, что не превышен rate limit (1 час)
3. Проверьте токен авторизации

### Не удается удалить аккаунт

1. Проверьте наличие активных заказов
2. Убедитесь, что баланс кошелька = 0
3. Проверьте правильность пароля

### Согласия не сохраняются

1. Проверьте подключение к интернету
2. Убедитесь, что пользователь авторизован
3. Проверьте логи на ошибки синхронизации

## Поддержка

Для вопросов по GDPR:
- Email: privacy@handshakeme.com
- Политика конфиденциальности: https://handshakeme.com/privacy-policy

## Лицензия

Этот модуль является частью HandShakeMe и защищен соответствующей лицензией.
